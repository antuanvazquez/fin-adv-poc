# Infrastructure as Code

Terraform modules are not yet implemented. The POC runs locally via `npm run dev`.

For the production AWS architecture, see `../../infrastructure_plan.md` — covers VPC, ECS Fargate, RDS PostgreSQL, EventBridge, SQS, Bedrock, and full compliance stack.
