# Backend Services

Backend API services are not yet implemented. The POC uses Next.js API routes (Route Handlers) for server-side operations including Claude AI integration.

For the production architecture, see `../../infrastructure_plan.md` (Core API Service, AI Worker, Communication Worker, Event Processor).
